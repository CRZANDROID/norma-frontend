# Vite carga este archivo en `pnpm dev` (prioridad sobre el `.env` local).
# Mock off = Nest real. Para preview UI sin API, crea `.env.development.local`
# con VITE_USE_API_MOCK=true (ese archivo no se sube a git).
VITE_API_URL=http://localhost:3000
VITE_USE_API_MOCK=false
VITE_DESIGN_PREVIEW=false
