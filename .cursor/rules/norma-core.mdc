---
description: NORMA frontend core — architecture, auth boundary, no hand-written CSS
alwaysApply: true
---

# NORMA — Frontend Core

Antes de codear: `docs/README.md`, `docs/PRODUCT.md`, `docs/ARCHITECTURE.md`, `docs/SPRINTS.md`.
Contratos de API: repo `norma-backend` → `docs/README.md` (no copiar Postman en este repo).

## Arquitectura de archivos

`app/` (shell/router) + `features/<dominio>/` + `shared/`.  
No agregar pantallas nuevas en `src/pages/` plano. Ver `ARCHITECTURE.md`.

## Estilos

- Tailwind + Radix + CVA. **Cero CSS a mano.**
- Motion/UI libs (`motion`, `sonner`, etc.) cuando aporten.

## Datos

- Auth = Nest JWT (`POST /auth/login` + Bearer).
- Negocio = NestJS + Bearer.
- No PostgREST a tablas de negocio.
- Mocks solo temporales; no permanentes si el API del sprint existe.

## Sprint actual

Siguiente: S8 loop en `/alertas` (editar / IA / excluir; **sin PDF**). Contrato: backend `docs/FRONTEND-ALERTAS.md`.
YouTube / X / Facebook = **MVP después de S10**: backend `docs/PRODUCT.md`.
Admin: `docs/DESIGN-CLIENTS-PROFILES.md`. Contrato API: backend `docs/FRONTEND-ADMIN.md`.
